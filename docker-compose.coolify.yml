services:
  hermes-gateway:
    image: nousresearch/hermes-agent:latest
    restart: unless-stopped
    volumes:
      - hermes_data:/opt/data
    command: ["gateway", "run"]
    expose:
      - "8642"

volumes:
  hermes_data:
